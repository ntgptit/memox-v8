tags:
  golden:
    description: >-
      Pixel comparisons with committed PNGs, generated in the Linux container
      of .claude/skills/flutter-testing/scripts/golden.Dockerfile (spec §8.2).
      Regenerate there with: TZ=UTC flutter test --tags golden --update-goldens
