---
target: kit screens 11 Card import and 12 Card export (pre-plan)
total_score: 31
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
target_identity: "file:/tmp/claude-0/-home-user-memox-v8/76b617be-c32f-5cc8-9a98-8689c9dbc981/scratchpad/transfer-kit"
timestamp: 2026-09-26T03-27-35Z
slug: transfer-kit
---
Method: dual-agent (A: design review · B: detector + browser)

Target: MemoX UI Kit v3 — screen 11 Card import (16 states) and screen 12 Card export (9 states), plus entry points (deckActions, empty card list, empty deck, bulk bar). Reviewed as specified design before planning; no Flutter build yet.

## Design Health Score: 31/40 (Good, 78%)

| # | Heuristic | Score | Key issue |
|---|---|---|---|
| 1 | Visibility of system status | 4 | Every gate says what has and has not happened ("Reading — no changes yet") |
| 2 | Match system / real world | 3 | Two vocabularies for the same fields: term/meaning (helper) vs front/back (mapping targets) |
| 3 | User control and freedom | 3 | Back/Cancel/choose another file everywhere; no "Import another file" exit (UC-TRANSFER-001 step 8) |
| 4 | Consistency and standards | 3 | Result screen hand-builds a hero card instead of the handoff's EmptyState/ErrorState tones |
| 5 | Error prevention | 4 | mappingIncomplete and zero will-write lock Continue before any write; duplicates skipped by default |
| 6 | Recognition vs recall | 3 | Deck chip always visible; original header name shown only as small monospace |
| 7 | Flexibility and efficiency | 2 | No remembered mapping for a recurring spreadsheet shape |
| 8 | Aesthetic and minimalist | 3 | "1 · Choose a source" (two big buttons) stays on screen through mapping, preview and importing |
| 9 | Error recovery | 4 | Every failure names cause + fix + "nothing was lost" (local-first voice) |
| 10 | Help and documentation | 2 | No example file; A3 (no header row) has no kit state |

## Design specificity
Authored for MemoX, not category-interchangeable: the copy carries the rules (new cards have no schedule, all or nothing, "handed to the system" never "saved", `none` and `rejects` results). Step tracker, dashed drop card and mapping pickers are generic importer patterns but a minority.
Detector: CLI scan of the 18 JSX modules is clean (0 findings). Browser overlay on the rendered kit: on screen 11 a nested-card badge on the dashed "Pick a spreadsheet" card inside the source card, and a low-contrast badge on its caption (#7a7f92 on #e6e8ea, 3.2:1). The other page-wide findings (10px labels, single font, clipped `.phone`) belong to the kit's own chrome, not to these screens.

## Priority issues
- [P1] The source picker never collapses. "1 · Choose a source" is rendered unconditionally (DeckImportScreen.jsx), so the two big source buttons stay above mapping, preview and the commit; the helper banner does collapse. Fix: once a source is read, Step 1 shows only the file chip (with its remove/change action). /impeccable distill
- [P1] Large previews have no orientation. The table maps every row; the result sample adds 1,500 cards; UC-TRANSFER-001 step 5 already says "the first rows". Fix: render the first N rows and a caption "Showing first N of M rows" tied to the summary badges. /impeccable harden
- [P2] Status by icon colour. The ready row's check icon has no label; an unmapped column is marked by the picker's red border only; preview cells ellipsize, which at large text scale can hide the term the user must judge. Fix: semantic labels per status, the mapping error on its own row, cells that wrap to two lines. /impeccable audit
- [P2] Result screen reinvents a component. RESULT_CFG builds its own coloured hero for success/partial/none/failed/rejects; the handoff owns EmptyState/ErrorState with success/neutral/warning/danger tones. Fix: compose the Mx components. /impeccable polish
- [P3] Vocabulary drift (term/meaning vs Term (front)/Meaning (back)); tracker shows 4 steps (Source · Columns · Preview · Import) while the UC says 3; no "Import another file" exit. /impeccable clarify

## Persona red flags
- Jordan (first-timer): a file without a header row (A3) has no designed state; no example file; two names for the same field.
- Sam (TalkBack): unlabeled ready icon, colour-only mapping error, ellipsized cells at large text scale.
- Casey (one-handed): Step 1 buttons push the table and commit down at every step; 4 pickers in a row at mapping.

## Minor observations
- deckActions shows "Export all N cards" unconditionally; UC-TRANSFER-002 E5 hides the entry point on an empty deck (UC wins).
- previewMix says duplicates follow "the option below": name the option instead.
- `importing` has an indeterminate spinner only; add "large imports can take a moment".
- 48 generated design-handoff widget files carry a stray `/superpowers:subagent-driven-development` line after the DO-NOT-EDIT header (generator artifact, outside this work).

## §8.1 rulings
1. `none`: keep. It is a commit-time outcome (the duplicate re-check inside the transaction finds nothing to write), distinct from E3 at preview. UC-TRANSFER-001 gains it as an error flow; the kit copy stays ("Nothing added — every row already exists in this deck… The deck is unchanged."), neutral tone, one exit back to the deck.
2. XLSX sheet choice (A2): no new state. A "Sheet: <name> (1 of 3)" row on the file chip, shown only for multi-sheet workbooks; changing it reruns mapping and preview like changing the file. One golden variant of fileSelected/mapping.
3. Entry points: consistent. Import from deckActions, the empty card list and the empty deck's third option; export from deckActions and the bulk bar's Export (FlashcardListScreen MAIN, bulk bar Move · Flag · Tag · Export · Trash). Nothing missing; only the empty-deck rule above.

## Questions to consider
- Is this a wizard or one long form with a tracker? If a wizard, Step 1 must collapse.
- Would a remembered mapping for the owner's own recurring spreadsheet beat any visual change here?
