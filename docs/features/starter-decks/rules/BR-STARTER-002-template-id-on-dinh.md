---
id: BR-STARTER-002
title: template_id ổn định
status: active
summary: Template có `template_id` ổn định giữa các phiên bản app, kèm `version`, `locale`, `title`, `content_source`.
superseded_by:
---
## Rule

Template MUST có `template_id` ổn định không đổi giữa các phiên bản app, kèm `version`, `locale`, `title`, `content_source`.

**Enforced by:** asset

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
