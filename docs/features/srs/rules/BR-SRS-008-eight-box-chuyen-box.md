---
id: BR-SRS-008
title: eight_box: chuyển box
status: active
summary: `eight_box`: `forgotten` về box 1, `remembered` lên `min(8, current_box + 1)`.
superseded_by:
---
## Rule

Hai action: `forgotten` và `remembered`.

| Action | Box đích |
|---|---|
| `forgotten` | `1` |
| `remembered` | `min(8, current_box + 1)` |

**Enforced by:** rule

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
