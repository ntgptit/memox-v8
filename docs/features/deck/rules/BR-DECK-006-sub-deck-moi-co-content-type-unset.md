---
id: BR-DECK-006
title: Sub-deck mới có content_type unset
status: active
summary: Sub-deck mới tạo có `content_type = unset`; người dùng không chọn `content_type` khi tạo.
superseded_by:
---
## Rule

Sub-deck mới tạo MUST có `content_type = unset`. Người dùng MUST NOT chọn `content_type` khi tạo.

**Enforced by:** rule

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
