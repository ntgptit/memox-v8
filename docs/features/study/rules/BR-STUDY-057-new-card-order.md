---
id: BR-STUDY-057
title: new_card_order
status: active
summary: `new_card_order` là `created` hoặc `random`, mặc định `created`.
superseded_by:
---
## Rule

`new_card_order` MUST là một trong hai: `created` (theo `created_at` tăng dần) hoặc `random`. Mặc định `created`.

**Enforced by:** rule
**Liên quan:** BR-STUDY-002, BR-STUDY-056

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
