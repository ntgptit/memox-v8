---
id: BR-STUDY-052
title: kind learning cho chuỗi học mới
status: active
summary: `kind = learning` dành cho lượt trong chuỗi học mới; không xuất hiện trong phiên `reviewing`.
superseded_by:
---
## Rule

`kind = 'learning'` MUST dành cho lượt trong chuỗi học mới: ghi lịch sử, không đổi lịch. MUST NOT xuất hiện trong phiên `reviewing`.

**Enforced by:** db
**Liên quan:** BR-SRS-014, BR-STUDY-023

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
