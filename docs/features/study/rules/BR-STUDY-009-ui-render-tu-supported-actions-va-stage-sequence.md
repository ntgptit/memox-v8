---
id: BR-STUDY-009
title: UI render từ supportedActions và stageSequence
status: active
summary: Nút đánh giá và chuỗi stage render từ scheduler của root deck, không hardcode.
superseded_by:
---
## Rule

UI MUST render nút đánh giá từ `supportedActions`, và chuỗi stage từ `stageSequence`, của scheduler thuộc root deck; MUST NOT hardcode tập nào trong hai.

**Enforced by:** UI
**Liên quan:** BR-MODE-007

## Lý do

Nguồn không ghi lý do.

## Ví dụ

Không áp dụng

## Edge case

Không áp dụng
